<%= node['oracle']['sid'] %> =
  (description =
    (address_list =
      (address = (protocol = tcp)(host = <%= node['oracle']['sid'] %>.sbg-qbo-<%= node['oracle']['vpc_env'] %>.a.intuit.com)(port = 1521))
    )
    (connect_data =
      (sid = <%= node['oracle']['sid'] %>)
    )
  )

