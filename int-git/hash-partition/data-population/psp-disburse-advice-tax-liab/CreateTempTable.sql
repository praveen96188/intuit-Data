CREATE TABLE PSPADM.temp_company_disburse
(
    id integer GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL PRIMARY KEY,
    COMPANY_FK    VARCHAR2(255) NOT NULL,
    STATUS_CD NUMBER(1) DEFAULT 0
);