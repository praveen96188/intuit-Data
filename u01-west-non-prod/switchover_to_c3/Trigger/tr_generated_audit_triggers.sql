-- This is autogenerated file and created during build by codegen AuditTriggerGenerator.java file
-- This file is kept here to build postgres docker image and to be deleted later
CREATE OR REPLACE FUNCTION FN_PSP_BPCOMPANY_SERVICE_INFO_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT CREATOR_ID INTO v_creator_id FROM PSP_COMPANY_SERVICE WHERE COMPANY_SERVICE_SEQ = NEW.BPCOMPANY_SERVICE_INFO_SEQ;
SELECT MODIFIER_ID INTO v_modifier_id FROM PSP_COMPANY_SERVICE WHERE COMPANY_SERVICE_SEQ = NEW.BPCOMPANY_SERVICE_INFO_SEQ;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT <> OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT) AND NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NULL AND OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_SERVICE t1 WHERE t1.COMPANY_SERVICE_SEQ = NEW.BPCOMPANY_SERVICE_INFO_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'BPCompanyServiceInfo', 'OverrideCompanyLimitAmount', (OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT )::VARCHAR, (NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT )::VARCHAR, NEW.BPCOMPANY_SERVICE_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.OVERRIDE_PAYEE_LIMIT_AMOUNT <> OLD.OVERRIDE_PAYEE_LIMIT_AMOUNT) AND NEW.OVERRIDE_PAYEE_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_PAYEE_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_PAYEE_LIMIT_AMOUNT IS NULL AND OLD.OVERRIDE_PAYEE_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_PAYEE_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_PAYEE_LIMIT_AMOUNT IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_SERVICE t1 WHERE t1.COMPANY_SERVICE_SEQ = NEW.BPCOMPANY_SERVICE_INFO_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'BPCompanyServiceInfo', 'OverridePayeeLimitAmount', (OLD.OVERRIDE_PAYEE_LIMIT_AMOUNT )::VARCHAR, (NEW.OVERRIDE_PAYEE_LIMIT_AMOUNT )::VARCHAR, NEW.BPCOMPANY_SERVICE_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_BPCOMPANY_SERVICE_INFO_AT
AFTER INSERT OR UPDATE ON PSP_BPCOMPANY_SERVICE_INFO
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_BPCOMPANY_SERVICE_INFO_AT();


ALTER TABLE PSP_BPCOMPANY_SERVICE_INFO DISABLE TRIGGER PSP_BPCOMPANY_SERVICE_INFO_AT;
ALTER TABLE PSP_BPCOMPANY_SERVICE_INFO ENABLE TRIGGER PSP_BPCOMPANY_SERVICE_INFO_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_COMPANY_AGENCY_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.ER_FICA_DEFERRAL_ENABLED <> OLD.ER_FICA_DEFERRAL_ENABLED) AND NEW.ER_FICA_DEFERRAL_ENABLED IS NOT NULL AND OLD.ER_FICA_DEFERRAL_ENABLED IS NOT NULL) OR
         (NEW.ER_FICA_DEFERRAL_ENABLED IS NULL AND OLD.ER_FICA_DEFERRAL_ENABLED IS NOT NULL) OR
         (NEW.ER_FICA_DEFERRAL_ENABLED IS NOT NULL AND OLD.ER_FICA_DEFERRAL_ENABLED IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyAgency', 'ErFicaDeferralEnabled', (OLD.ER_FICA_DEFERRAL_ENABLED )::VARCHAR, (NEW.ER_FICA_DEFERRAL_ENABLED )::VARCHAR, NEW.COMPANY_AGENCY_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_COMPANY_AGENCY_AT
AFTER INSERT OR UPDATE ON PSP_COMPANY_AGENCY
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_COMPANY_AGENCY_AT();


ALTER TABLE PSP_COMPANY_AGENCY DISABLE TRIGGER PSP_COMPANY_AGENCY_AT;
ALTER TABLE PSP_COMPANY_AGENCY ENABLE TRIGGER PSP_COMPANY_AGENCY_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_CMPNYAGENCY_PMTTPLT_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.AGENCY_TAXPAYER_ID_ENC <> OLD.AGENCY_TAXPAYER_ID_ENC) AND NEW.AGENCY_TAXPAYER_ID_ENC IS NOT NULL AND OLD.AGENCY_TAXPAYER_ID_ENC IS NOT NULL) OR
         (NEW.AGENCY_TAXPAYER_ID_ENC IS NULL AND OLD.AGENCY_TAXPAYER_ID_ENC IS NOT NULL) OR
         (NEW.AGENCY_TAXPAYER_ID_ENC IS NOT NULL AND OLD.AGENCY_TAXPAYER_ID_ENC IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  WHERE t1.COMPANY_AGENCY_SEQ = NEW.COMPANY_AGENCY_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyAgencyPaymentTemplate', 'AgencyTaxpayerIdEnc', (OLD.AGENCY_TAXPAYER_ID_ENC )::VARCHAR, (NEW.AGENCY_TAXPAYER_ID_ENC )::VARCHAR, NEW.COMPANYAGENCY_PMTTEMPLATE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_CMPNYAGENCY_PMTTPLT_AT
AFTER INSERT OR UPDATE ON PSP_COMPANYAGENCY_PMTTEMPLATE
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_CMPNYAGENCY_PMTTPLT_AT();


ALTER TABLE PSP_COMPANYAGENCY_PMTTEMPLATE DISABLE TRIGGER PSP_CMPNYAGENCY_PMTTPLT_AT;
ALTER TABLE PSP_COMPANYAGENCY_PMTTEMPLATE ENABLE TRIGGER PSP_CMPNYAGENCY_PMTTPLT_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_COMPANY_LAW_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.EXEMPTION_STATUS <> OLD.EXEMPTION_STATUS) AND NEW.EXEMPTION_STATUS IS NOT NULL AND OLD.EXEMPTION_STATUS IS NOT NULL) OR
         (NEW.EXEMPTION_STATUS IS NULL AND OLD.EXEMPTION_STATUS IS NOT NULL) OR
         (NEW.EXEMPTION_STATUS IS NOT NULL AND OLD.EXEMPTION_STATUS IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  WHERE t1.COMPANY_AGENCY_SEQ = NEW.COMPANY_AGENCY_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyLaw', 'ExemptionStatus', (OLD.EXEMPTION_STATUS )::VARCHAR, (NEW.EXEMPTION_STATUS )::VARCHAR, NEW.COMPANY_LAW_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.FILING_STATUS <> OLD.FILING_STATUS) AND NEW.FILING_STATUS IS NOT NULL AND OLD.FILING_STATUS IS NOT NULL) OR
         (NEW.FILING_STATUS IS NULL AND OLD.FILING_STATUS IS NOT NULL) OR
         (NEW.FILING_STATUS IS NOT NULL AND OLD.FILING_STATUS IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  WHERE t1.COMPANY_AGENCY_SEQ = NEW.COMPANY_AGENCY_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyLaw', 'FilingStatus', (OLD.FILING_STATUS )::VARCHAR, (NEW.FILING_STATUS )::VARCHAR, NEW.COMPANY_LAW_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.REIMBURSABLE_STATUS <> OLD.REIMBURSABLE_STATUS) AND NEW.REIMBURSABLE_STATUS IS NOT NULL AND OLD.REIMBURSABLE_STATUS IS NOT NULL) OR
         (NEW.REIMBURSABLE_STATUS IS NULL AND OLD.REIMBURSABLE_STATUS IS NOT NULL) OR
         (NEW.REIMBURSABLE_STATUS IS NOT NULL AND OLD.REIMBURSABLE_STATUS IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  WHERE t1.COMPANY_AGENCY_SEQ = NEW.COMPANY_AGENCY_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyLaw', 'ReimbursableStatus', (OLD.REIMBURSABLE_STATUS )::VARCHAR, (NEW.REIMBURSABLE_STATUS )::VARCHAR, NEW.COMPANY_LAW_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_COMPANY_LAW_AT
AFTER INSERT OR UPDATE ON PSP_COMPANY_LAW
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_COMPANY_LAW_AT();


ALTER TABLE PSP_COMPANY_LAW DISABLE TRIGGER PSP_COMPANY_LAW_AT;
ALTER TABLE PSP_COMPANY_LAW ENABLE TRIGGER PSP_COMPANY_LAW_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_COMPANY_OFFER_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.OFFER_FK <> OLD.OFFER_FK) AND NEW.OFFER_FK IS NOT NULL AND OLD.OFFER_FK IS NOT NULL) OR
         (NEW.OFFER_FK IS NULL AND OLD.OFFER_FK IS NOT NULL) OR
         (NEW.OFFER_FK IS NOT NULL AND OLD.OFFER_FK IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyOffer', 'Offer', (OLD.OFFER_FK )::VARCHAR, (NEW.OFFER_FK )::VARCHAR, NEW.COMPANY_OFFER_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_COMPANY_OFFER_AT
AFTER INSERT OR UPDATE ON PSP_COMPANY_OFFER
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_COMPANY_OFFER_AT();


ALTER TABLE PSP_COMPANY_OFFER DISABLE TRIGGER PSP_COMPANY_OFFER_AT;
ALTER TABLE PSP_COMPANY_OFFER ENABLE TRIGGER PSP_COMPANY_OFFER_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_CMPMTMPLT_PMTMTD_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.AGENT_ENABLED <> OLD.AGENT_ENABLED) AND NEW.AGENT_ENABLED IS NOT NULL AND OLD.AGENT_ENABLED IS NOT NULL) OR
         (NEW.AGENT_ENABLED IS NULL AND OLD.AGENT_ENABLED IS NOT NULL) OR
         (NEW.AGENT_ENABLED IS NOT NULL AND OLD.AGENT_ENABLED IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  JOIN PSP_COMPANYAGENCY_PMTTEMPLATE capt ON t1.COMPANY_AGENCY_SEQ = capt.COMPANY_AGENCY_FK WHERE capt.COMPANYAGENCY_PMTTEMPLATE_SEQ = NEW.COMPANY_AGENCY_PMT_TEMPLATE_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyPaymentTemplatePaymentMethod', 'AgentEnabled', (OLD.AGENT_ENABLED )::VARCHAR, (NEW.AGENT_ENABLED )::VARCHAR, NEW.COMP_PMTTEMPLATE_PMTMETHOD_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.ENABLED <> OLD.ENABLED) AND NEW.ENABLED IS NOT NULL AND OLD.ENABLED IS NOT NULL) OR
         (NEW.ENABLED IS NULL AND OLD.ENABLED IS NOT NULL) OR
         (NEW.ENABLED IS NOT NULL AND OLD.ENABLED IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  JOIN PSP_COMPANYAGENCY_PMTTEMPLATE capt ON t1.COMPANY_AGENCY_SEQ = capt.COMPANY_AGENCY_FK WHERE capt.COMPANYAGENCY_PMTTEMPLATE_SEQ = NEW.COMPANY_AGENCY_PMT_TEMPLATE_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyPaymentTemplatePaymentMethod', 'Enabled', (OLD.ENABLED )::VARCHAR, (NEW.ENABLED )::VARCHAR, NEW.COMP_PMTTEMPLATE_PMTMETHOD_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_CMPMTMPLT_PMTMTD_AT
AFTER INSERT OR UPDATE ON PSP_COMP_PMTTEMPLATE_PMTMETHOD
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_CMPMTMPLT_PMTMTD_AT();


ALTER TABLE PSP_COMP_PMTTEMPLATE_PMTMETHOD DISABLE TRIGGER PSP_CMPMTMPLT_PMTMTD_AT;
ALTER TABLE PSP_COMP_PMTTEMPLATE_PMTMETHOD ENABLE TRIGGER PSP_CMPMTMPLT_PMTMTD_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_COMPANY_PAYROLL_ITEM_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.PAYROLL_ITEM_FK <> OLD.PAYROLL_ITEM_FK) AND NEW.PAYROLL_ITEM_FK IS NOT NULL AND OLD.PAYROLL_ITEM_FK IS NOT NULL) OR
         (NEW.PAYROLL_ITEM_FK IS NULL AND OLD.PAYROLL_ITEM_FK IS NOT NULL) OR
         (NEW.PAYROLL_ITEM_FK IS NOT NULL AND OLD.PAYROLL_ITEM_FK IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyPayrollItem', 'PayrollItem', (OLD.PAYROLL_ITEM_FK )::VARCHAR, (NEW.PAYROLL_ITEM_FK )::VARCHAR, NEW.COMPANY_PAYROLL_ITEM_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.SOURCE_DESCRIPTION <> OLD.SOURCE_DESCRIPTION) AND NEW.SOURCE_DESCRIPTION IS NOT NULL AND OLD.SOURCE_DESCRIPTION IS NOT NULL) OR
         (NEW.SOURCE_DESCRIPTION IS NULL AND OLD.SOURCE_DESCRIPTION IS NOT NULL) OR
         (NEW.SOURCE_DESCRIPTION IS NOT NULL AND OLD.SOURCE_DESCRIPTION IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyPayrollItem', 'SourceDescription', (OLD.SOURCE_DESCRIPTION )::VARCHAR, (NEW.SOURCE_DESCRIPTION )::VARCHAR, NEW.COMPANY_PAYROLL_ITEM_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.SOURCE_PAYROLL_ITEM_ID <> OLD.SOURCE_PAYROLL_ITEM_ID) AND NEW.SOURCE_PAYROLL_ITEM_ID IS NOT NULL AND OLD.SOURCE_PAYROLL_ITEM_ID IS NOT NULL) OR
         (NEW.SOURCE_PAYROLL_ITEM_ID IS NULL AND OLD.SOURCE_PAYROLL_ITEM_ID IS NOT NULL) OR
         (NEW.SOURCE_PAYROLL_ITEM_ID IS NOT NULL AND OLD.SOURCE_PAYROLL_ITEM_ID IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyPayrollItem', 'SourcePayrollItemId', (OLD.SOURCE_PAYROLL_ITEM_ID )::VARCHAR, (NEW.SOURCE_PAYROLL_ITEM_ID )::VARCHAR, NEW.COMPANY_PAYROLL_ITEM_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_COMPANY_PAYROLL_ITEM_AT
AFTER INSERT OR UPDATE ON PSP_COMPANY_PAYROLL_ITEM
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_COMPANY_PAYROLL_ITEM_AT();


ALTER TABLE PSP_COMPANY_PAYROLL_ITEM DISABLE TRIGGER PSP_COMPANY_PAYROLL_ITEM_AT;
ALTER TABLE PSP_COMPANY_PAYROLL_ITEM ENABLE TRIGGER PSP_COMPANY_PAYROLL_ITEM_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_ENTITY_CHANGE_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.AGENT_ID <> OLD.AGENT_ID) AND NEW.AGENT_ID IS NOT NULL AND OLD.AGENT_ID IS NOT NULL) OR
         (NEW.AGENT_ID IS NULL AND OLD.AGENT_ID IS NOT NULL) OR
         (NEW.AGENT_ID IS NOT NULL AND OLD.AGENT_ID IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'EntityChange', 'AgentId', (OLD.AGENT_ID )::VARCHAR, (NEW.AGENT_ID )::VARCHAR, NEW.ENTITY_CHANGE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.EFFECTIVE_DATE <> OLD.EFFECTIVE_DATE) AND NEW.EFFECTIVE_DATE IS NOT NULL AND OLD.EFFECTIVE_DATE IS NOT NULL) OR
         (NEW.EFFECTIVE_DATE IS NULL AND OLD.EFFECTIVE_DATE IS NOT NULL) OR
         (NEW.EFFECTIVE_DATE IS NOT NULL AND OLD.EFFECTIVE_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'EntityChange', 'EffectiveDate', (OLD.EFFECTIVE_DATE )::VARCHAR, (NEW.EFFECTIVE_DATE )::VARCHAR, NEW.ENTITY_CHANGE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.NEW_EIN_ENC <> OLD.NEW_EIN_ENC) AND NEW.NEW_EIN_ENC IS NOT NULL AND OLD.NEW_EIN_ENC IS NOT NULL) OR
         (NEW.NEW_EIN_ENC IS NULL AND OLD.NEW_EIN_ENC IS NOT NULL) OR
         (NEW.NEW_EIN_ENC IS NOT NULL AND OLD.NEW_EIN_ENC IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'EntityChange', 'NewEinEnc', (OLD.NEW_EIN_ENC )::VARCHAR, (NEW.NEW_EIN_ENC )::VARCHAR, NEW.ENTITY_CHANGE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.OLD_EIN_ENC <> OLD.OLD_EIN_ENC) AND NEW.OLD_EIN_ENC IS NOT NULL AND OLD.OLD_EIN_ENC IS NOT NULL) OR
         (NEW.OLD_EIN_ENC IS NULL AND OLD.OLD_EIN_ENC IS NOT NULL) OR
         (NEW.OLD_EIN_ENC IS NOT NULL AND OLD.OLD_EIN_ENC IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'EntityChange', 'OldEinEnc', (OLD.OLD_EIN_ENC )::VARCHAR, (NEW.OLD_EIN_ENC )::VARCHAR, NEW.ENTITY_CHANGE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_ENTITY_CHANGE_AT
AFTER INSERT OR UPDATE ON PSP_ENTITY_CHANGE
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_ENTITY_CHANGE_AT();


ALTER TABLE PSP_ENTITY_CHANGE DISABLE TRIGGER PSP_ENTITY_CHANGE_AT;
ALTER TABLE PSP_ENTITY_CHANGE ENABLE TRIGGER PSP_ENTITY_CHANGE_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_QUICKBOOKS_INFO_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.APPLICATION_ID <> OLD.APPLICATION_ID) AND NEW.APPLICATION_ID IS NOT NULL AND OLD.APPLICATION_ID IS NOT NULL) OR
         (NEW.APPLICATION_ID IS NULL AND OLD.APPLICATION_ID IS NOT NULL) OR
         (NEW.APPLICATION_ID IS NOT NULL AND OLD.APPLICATION_ID IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'ApplicationId', (OLD.APPLICATION_ID )::VARCHAR, (NEW.APPLICATION_ID )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.APPLICATION_VERSION <> OLD.APPLICATION_VERSION) AND NEW.APPLICATION_VERSION IS NOT NULL AND OLD.APPLICATION_VERSION IS NOT NULL) OR
         (NEW.APPLICATION_VERSION IS NULL AND OLD.APPLICATION_VERSION IS NOT NULL) OR
         (NEW.APPLICATION_VERSION IS NOT NULL AND OLD.APPLICATION_VERSION IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'ApplicationVersion', (OLD.APPLICATION_VERSION )::VARCHAR, (NEW.APPLICATION_VERSION )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.A_S400_PAYROLL_COUNT <> OLD.A_S400_PAYROLL_COUNT) AND NEW.A_S400_PAYROLL_COUNT IS NOT NULL AND OLD.A_S400_PAYROLL_COUNT IS NOT NULL) OR
         (NEW.A_S400_PAYROLL_COUNT IS NULL AND OLD.A_S400_PAYROLL_COUNT IS NOT NULL) OR
         (NEW.A_S400_PAYROLL_COUNT IS NOT NULL AND OLD.A_S400_PAYROLL_COUNT IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'AS400PayrollCount', (OLD.A_S400_PAYROLL_COUNT )::VARCHAR, (NEW.A_S400_PAYROLL_COUNT )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.COA_FEE_ACCOUNT_NAME <> OLD.COA_FEE_ACCOUNT_NAME) AND NEW.COA_FEE_ACCOUNT_NAME IS NOT NULL AND OLD.COA_FEE_ACCOUNT_NAME IS NOT NULL) OR
         (NEW.COA_FEE_ACCOUNT_NAME IS NULL AND OLD.COA_FEE_ACCOUNT_NAME IS NOT NULL) OR
         (NEW.COA_FEE_ACCOUNT_NAME IS NOT NULL AND OLD.COA_FEE_ACCOUNT_NAME IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'CoaFeeAccountName', (OLD.COA_FEE_ACCOUNT_NAME )::VARCHAR, (NEW.COA_FEE_ACCOUNT_NAME )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.COA_SALES_TAX_ACCOUNT_NAME <> OLD.COA_SALES_TAX_ACCOUNT_NAME) AND NEW.COA_SALES_TAX_ACCOUNT_NAME IS NOT NULL AND OLD.COA_SALES_TAX_ACCOUNT_NAME IS NOT NULL) OR
         (NEW.COA_SALES_TAX_ACCOUNT_NAME IS NULL AND OLD.COA_SALES_TAX_ACCOUNT_NAME IS NOT NULL) OR
         (NEW.COA_SALES_TAX_ACCOUNT_NAME IS NOT NULL AND OLD.COA_SALES_TAX_ACCOUNT_NAME IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'CoaSalesTaxAccountName', (OLD.COA_SALES_TAX_ACCOUNT_NAME )::VARCHAR, (NEW.COA_SALES_TAX_ACCOUNT_NAME )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.LICENSE_NUMBER <> OLD.LICENSE_NUMBER) AND NEW.LICENSE_NUMBER IS NOT NULL AND OLD.LICENSE_NUMBER IS NOT NULL) OR
         (NEW.LICENSE_NUMBER IS NULL AND OLD.LICENSE_NUMBER IS NOT NULL) OR
         (NEW.LICENSE_NUMBER IS NOT NULL AND OLD.LICENSE_NUMBER IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'LicenseNumber', (OLD.LICENSE_NUMBER )::VARCHAR, (NEW.LICENSE_NUMBER )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TAX_TABLE_ID <> OLD.TAX_TABLE_ID) AND NEW.TAX_TABLE_ID IS NOT NULL AND OLD.TAX_TABLE_ID IS NOT NULL) OR
         (NEW.TAX_TABLE_ID IS NULL AND OLD.TAX_TABLE_ID IS NOT NULL) OR
         (NEW.TAX_TABLE_ID IS NOT NULL AND OLD.TAX_TABLE_ID IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'TaxTableId', (OLD.TAX_TABLE_ID )::VARCHAR, (NEW.TAX_TABLE_ID )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.FILE_ID <> OLD.FILE_ID) AND NEW.FILE_ID IS NOT NULL AND OLD.FILE_ID IS NOT NULL) OR
         (NEW.FILE_ID IS NULL AND OLD.FILE_ID IS NOT NULL) OR
         (NEW.FILE_ID IS NOT NULL AND OLD.FILE_ID IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'QuickbooksInfo', 'FileId', (OLD.FILE_ID )::VARCHAR, (NEW.FILE_ID )::VARCHAR, NEW.QUICKBOOKS_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_QUICKBOOKS_INFO_AT
AFTER INSERT OR UPDATE ON PSP_QUICKBOOKS_INFO
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_QUICKBOOKS_INFO_AT();


ALTER TABLE PSP_QUICKBOOKS_INFO DISABLE TRIGGER PSP_QUICKBOOKS_INFO_AT;
ALTER TABLE PSP_QUICKBOOKS_INFO ENABLE TRIGGER PSP_QUICKBOOKS_INFO_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_TAX_CS_INFO_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT CREATOR_ID INTO v_creator_id FROM PSP_COMPANY_SERVICE WHERE COMPANY_SERVICE_SEQ = NEW.TAX_COMPANY_SERVICE_INFO_SEQ;
SELECT MODIFIER_ID INTO v_modifier_id FROM PSP_COMPANY_SERVICE WHERE COMPANY_SERVICE_SEQ = NEW.TAX_COMPANY_SERVICE_INFO_SEQ;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.W2_DELIVERY_PREFERENCE_CD <> OLD.W2_DELIVERY_PREFERENCE_CD) AND NEW.W2_DELIVERY_PREFERENCE_CD IS NOT NULL AND OLD.W2_DELIVERY_PREFERENCE_CD IS NOT NULL) OR
         (NEW.W2_DELIVERY_PREFERENCE_CD IS NULL AND OLD.W2_DELIVERY_PREFERENCE_CD IS NOT NULL) OR
         (NEW.W2_DELIVERY_PREFERENCE_CD IS NOT NULL AND OLD.W2_DELIVERY_PREFERENCE_CD IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_SERVICE t1 WHERE t1.COMPANY_SERVICE_SEQ = NEW.TAX_COMPANY_SERVICE_INFO_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'TaxCompanyServiceInfo', 'W2DeliveryPreferenceCd', (OLD.W2_DELIVERY_PREFERENCE_CD )::VARCHAR, (NEW.W2_DELIVERY_PREFERENCE_CD )::VARCHAR, NEW.TAX_COMPANY_SERVICE_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_TAX_CS_INFO_AT
AFTER INSERT OR UPDATE ON PSP_TAX_COMPANY_SERVICE_INFO
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_TAX_CS_INFO_AT();


ALTER TABLE PSP_TAX_COMPANY_SERVICE_INFO DISABLE TRIGGER PSP_TAX_CS_INFO_AT;
ALTER TABLE PSP_TAX_COMPANY_SERVICE_INFO ENABLE TRIGGER PSP_TAX_CS_INFO_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_TAX_PENALTY_INTEREST_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.PAYMENT_METHOD <> OLD.PAYMENT_METHOD) AND NEW.PAYMENT_METHOD IS NOT NULL AND OLD.PAYMENT_METHOD IS NOT NULL) OR
         (NEW.PAYMENT_METHOD IS NULL AND OLD.PAYMENT_METHOD IS NOT NULL) OR
         (NEW.PAYMENT_METHOD IS NOT NULL AND OLD.PAYMENT_METHOD IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  WHERE t1.COMPANY_AGENCY_SEQ = NEW.COMPANY_AGENCY_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'TaxPenaltyInterest', 'PaymentMethod', (OLD.PAYMENT_METHOD )::VARCHAR, (NEW.PAYMENT_METHOD )::VARCHAR, NEW.TAX_PENALTY_INTEREST_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.AMOUNT <> OLD.AMOUNT) AND NEW.AMOUNT IS NOT NULL AND OLD.AMOUNT IS NOT NULL) OR
         (NEW.AMOUNT IS NULL AND OLD.AMOUNT IS NOT NULL) OR
         (NEW.AMOUNT IS NOT NULL AND OLD.AMOUNT IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_AGENCY t1  WHERE t1.COMPANY_AGENCY_SEQ = NEW.COMPANY_AGENCY_FK;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'TaxPenaltyInterest', 'Amount', (OLD.AMOUNT )::VARCHAR, (NEW.AMOUNT )::VARCHAR, NEW.TAX_PENALTY_INTEREST_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_TAX_PENALTY_INTEREST_AT
AFTER INSERT OR UPDATE ON PSP_TAX_PENALTY_INTEREST
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_TAX_PENALTY_INTEREST_AT();


ALTER TABLE PSP_TAX_PENALTY_INTEREST DISABLE TRIGGER PSP_TAX_PENALTY_INTEREST_AT;
ALTER TABLE PSP_TAX_PENALTY_INTEREST ENABLE TRIGGER PSP_TAX_PENALTY_INTEREST_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_ADDRESS_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.ADDRESS_LINE1 <> OLD.ADDRESS_LINE1) AND NEW.ADDRESS_LINE1 IS NOT NULL AND OLD.ADDRESS_LINE1 IS NOT NULL) OR
         (NEW.ADDRESS_LINE1 IS NULL AND OLD.ADDRESS_LINE1 IS NOT NULL) OR
         (NEW.ADDRESS_LINE1 IS NOT NULL AND OLD.ADDRESS_LINE1 IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'AddressLine1', (OLD.ADDRESS_LINE1 )::VARCHAR, (NEW.ADDRESS_LINE1 )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.ADDRESS_LINE2 <> OLD.ADDRESS_LINE2) AND NEW.ADDRESS_LINE2 IS NOT NULL AND OLD.ADDRESS_LINE2 IS NOT NULL) OR
         (NEW.ADDRESS_LINE2 IS NULL AND OLD.ADDRESS_LINE2 IS NOT NULL) OR
         (NEW.ADDRESS_LINE2 IS NOT NULL AND OLD.ADDRESS_LINE2 IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'AddressLine2', (OLD.ADDRESS_LINE2 )::VARCHAR, (NEW.ADDRESS_LINE2 )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.ADDRESS_LINE3 <> OLD.ADDRESS_LINE3) AND NEW.ADDRESS_LINE3 IS NOT NULL AND OLD.ADDRESS_LINE3 IS NOT NULL) OR
         (NEW.ADDRESS_LINE3 IS NULL AND OLD.ADDRESS_LINE3 IS NOT NULL) OR
         (NEW.ADDRESS_LINE3 IS NOT NULL AND OLD.ADDRESS_LINE3 IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'AddressLine3', (OLD.ADDRESS_LINE3 )::VARCHAR, (NEW.ADDRESS_LINE3 )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.CITY <> OLD.CITY) AND NEW.CITY IS NOT NULL AND OLD.CITY IS NOT NULL) OR
         (NEW.CITY IS NULL AND OLD.CITY IS NOT NULL) OR
         (NEW.CITY IS NOT NULL AND OLD.CITY IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'City', (OLD.CITY )::VARCHAR, (NEW.CITY )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.COUNTRY <> OLD.COUNTRY) AND NEW.COUNTRY IS NOT NULL AND OLD.COUNTRY IS NOT NULL) OR
         (NEW.COUNTRY IS NULL AND OLD.COUNTRY IS NOT NULL) OR
         (NEW.COUNTRY IS NOT NULL AND OLD.COUNTRY IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'Country', (OLD.COUNTRY )::VARCHAR, (NEW.COUNTRY )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.STATE <> OLD.STATE) AND NEW.STATE IS NOT NULL AND OLD.STATE IS NOT NULL) OR
         (NEW.STATE IS NULL AND OLD.STATE IS NOT NULL) OR
         (NEW.STATE IS NOT NULL AND OLD.STATE IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'State', (OLD.STATE )::VARCHAR, (NEW.STATE )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.ZIP_CODE <> OLD.ZIP_CODE) AND NEW.ZIP_CODE IS NOT NULL AND OLD.ZIP_CODE IS NOT NULL) OR
         (NEW.ZIP_CODE IS NULL AND OLD.ZIP_CODE IS NOT NULL) OR
         (NEW.ZIP_CODE IS NOT NULL AND OLD.ZIP_CODE IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'ZipCode', (OLD.ZIP_CODE )::VARCHAR, (NEW.ZIP_CODE )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.ZIP_CODE_EXTENSION <> OLD.ZIP_CODE_EXTENSION) AND NEW.ZIP_CODE_EXTENSION IS NOT NULL AND OLD.ZIP_CODE_EXTENSION IS NOT NULL) OR
         (NEW.ZIP_CODE_EXTENSION IS NULL AND OLD.ZIP_CODE_EXTENSION IS NOT NULL) OR
         (NEW.ZIP_CODE_EXTENSION IS NOT NULL AND OLD.ZIP_CODE_EXTENSION IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_SEQ INTO v_company_fk from PSP_COMPANY t1 JOIN PSP_EMPLOYEE ee ON t1.COMPANY_SEQ = ee.COMPANY_FK JOIN PSP_INDIVIDUAL individual on EE.EMPLOYEE_SEQ = INDIVIDUAL.INDIVIDUAL_SEQ WHERE (t1.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ or t1.LEGAL_ADDRESS_FK = NEW.ADDRESS_SEQ or INDIVIDUAL.MAILING_ADDRESS_FK = NEW.ADDRESS_SEQ) LIMIT 1;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Address', 'ZipCodeExtension', (OLD.ZIP_CODE_EXTENSION )::VARCHAR, (NEW.ZIP_CODE_EXTENSION )::VARCHAR, NEW.ADDRESS_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_ADDRESS_AT
AFTER INSERT OR UPDATE ON PSP_ADDRESS
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_ADDRESS_AT();


ALTER TABLE PSP_ADDRESS DISABLE TRIGGER PSP_ADDRESS_AT;
ALTER TABLE PSP_ADDRESS ENABLE TRIGGER PSP_ADDRESS_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_COMPANY_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.FUNDING_MODEL_FK <> OLD.FUNDING_MODEL_FK) AND NEW.FUNDING_MODEL_FK IS NOT NULL AND OLD.FUNDING_MODEL_FK IS NOT NULL) OR
         (NEW.FUNDING_MODEL_FK IS NULL AND OLD.FUNDING_MODEL_FK IS NOT NULL) OR
         (NEW.FUNDING_MODEL_FK IS NOT NULL AND OLD.FUNDING_MODEL_FK IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_SEQ INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Company', 'FundingModel', (OLD.FUNDING_MODEL_FK )::VARCHAR, (NEW.FUNDING_MODEL_FK )::VARCHAR, NEW.COMPANY_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.NOTIFICATION_EMAIL <> OLD.NOTIFICATION_EMAIL) AND NEW.NOTIFICATION_EMAIL IS NOT NULL AND OLD.NOTIFICATION_EMAIL IS NOT NULL) OR
         (NEW.NOTIFICATION_EMAIL IS NULL AND OLD.NOTIFICATION_EMAIL IS NOT NULL) OR
         (NEW.NOTIFICATION_EMAIL IS NOT NULL AND OLD.NOTIFICATION_EMAIL IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_SEQ INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Company', 'NotificationEmail', (OLD.NOTIFICATION_EMAIL )::VARCHAR, (NEW.NOTIFICATION_EMAIL )::VARCHAR, NEW.COMPANY_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.FED_TAX_ID_ENC <> OLD.FED_TAX_ID_ENC) AND NEW.FED_TAX_ID_ENC IS NOT NULL AND OLD.FED_TAX_ID_ENC IS NOT NULL) OR
         (NEW.FED_TAX_ID_ENC IS NULL AND OLD.FED_TAX_ID_ENC IS NOT NULL) OR
         (NEW.FED_TAX_ID_ENC IS NOT NULL AND OLD.FED_TAX_ID_ENC IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_SEQ INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Company', 'FedTaxIdEnc', (OLD.FED_TAX_ID_ENC )::VARCHAR, (NEW.FED_TAX_ID_ENC )::VARCHAR, NEW.COMPANY_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.LEGAL_NAME <> OLD.LEGAL_NAME) AND NEW.LEGAL_NAME IS NOT NULL AND OLD.LEGAL_NAME IS NOT NULL) OR
         (NEW.LEGAL_NAME IS NULL AND OLD.LEGAL_NAME IS NOT NULL) OR
         (NEW.LEGAL_NAME IS NOT NULL AND OLD.LEGAL_NAME IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_SEQ INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Company', 'LegalName', (OLD.LEGAL_NAME )::VARCHAR, (NEW.LEGAL_NAME )::VARCHAR, NEW.COMPANY_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_COMPANY_AT
AFTER INSERT OR UPDATE ON PSP_COMPANY
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_COMPANY_AT();


ALTER TABLE PSP_COMPANY DISABLE TRIGGER PSP_COMPANY_AT;
ALTER TABLE PSP_COMPANY ENABLE TRIGGER PSP_COMPANY_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_COMPANY_BANK_ACCOUNT_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.STATUS_CD <> OLD.STATUS_CD) AND NEW.STATUS_CD IS NOT NULL AND OLD.STATUS_CD IS NOT NULL) OR
         (NEW.STATUS_CD IS NULL AND OLD.STATUS_CD IS NOT NULL) OR
         (NEW.STATUS_CD IS NOT NULL AND OLD.STATUS_CD IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyBankAccount', 'StatusCd', (OLD.STATUS_CD )::VARCHAR, (NEW.STATUS_CD )::VARCHAR, NEW.COMPANY_BANK_ACCOUNT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.STATUS_EFFECTIVE_DATE <> OLD.STATUS_EFFECTIVE_DATE) AND NEW.STATUS_EFFECTIVE_DATE IS NOT NULL AND OLD.STATUS_EFFECTIVE_DATE IS NOT NULL) OR
         (NEW.STATUS_EFFECTIVE_DATE IS NULL AND OLD.STATUS_EFFECTIVE_DATE IS NOT NULL) OR
         (NEW.STATUS_EFFECTIVE_DATE IS NOT NULL AND OLD.STATUS_EFFECTIVE_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyBankAccount', 'StatusEffectiveDate', (OLD.STATUS_EFFECTIVE_DATE )::VARCHAR, (NEW.STATUS_EFFECTIVE_DATE )::VARCHAR, NEW.COMPANY_BANK_ACCOUNT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.SOURCE_BANK_ACCOUNT_NAME <> OLD.SOURCE_BANK_ACCOUNT_NAME) AND NEW.SOURCE_BANK_ACCOUNT_NAME IS NOT NULL AND OLD.SOURCE_BANK_ACCOUNT_NAME IS NOT NULL) OR
         (NEW.SOURCE_BANK_ACCOUNT_NAME IS NULL AND OLD.SOURCE_BANK_ACCOUNT_NAME IS NOT NULL) OR
         (NEW.SOURCE_BANK_ACCOUNT_NAME IS NOT NULL AND OLD.SOURCE_BANK_ACCOUNT_NAME IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyBankAccount', 'SourceBankAccountName', (OLD.SOURCE_BANK_ACCOUNT_NAME )::VARCHAR, (NEW.SOURCE_BANK_ACCOUNT_NAME )::VARCHAR, NEW.COMPANY_BANK_ACCOUNT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_COMPANY_BANK_ACCOUNT_AT
AFTER INSERT OR UPDATE ON PSP_COMPANY_BANK_ACCOUNT
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_COMPANY_BANK_ACCOUNT_AT();


ALTER TABLE PSP_COMPANY_BANK_ACCOUNT DISABLE TRIGGER PSP_COMPANY_BANK_ACCOUNT_AT;
ALTER TABLE PSP_COMPANY_BANK_ACCOUNT ENABLE TRIGGER PSP_COMPANY_BANK_ACCOUNT_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_COMPANY_SERVICE_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.STATUS_CD <> OLD.STATUS_CD) AND NEW.STATUS_CD IS NOT NULL AND OLD.STATUS_CD IS NOT NULL) OR
         (NEW.STATUS_CD IS NULL AND OLD.STATUS_CD IS NOT NULL) OR
         (NEW.STATUS_CD IS NOT NULL AND OLD.STATUS_CD IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyService', 'StatusCd', (OLD.STATUS_CD )::VARCHAR, (NEW.STATUS_CD )::VARCHAR, NEW.COMPANY_SERVICE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.STATUS_EFFECTIVE_DATE <> OLD.STATUS_EFFECTIVE_DATE) AND NEW.STATUS_EFFECTIVE_DATE IS NOT NULL AND OLD.STATUS_EFFECTIVE_DATE IS NOT NULL) OR
         (NEW.STATUS_EFFECTIVE_DATE IS NULL AND OLD.STATUS_EFFECTIVE_DATE IS NOT NULL) OR
         (NEW.STATUS_EFFECTIVE_DATE IS NOT NULL AND OLD.STATUS_EFFECTIVE_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'CompanyService', 'StatusEffectiveDate', (OLD.STATUS_EFFECTIVE_DATE )::VARCHAR, (NEW.STATUS_EFFECTIVE_DATE )::VARCHAR, NEW.COMPANY_SERVICE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_COMPANY_SERVICE_AT
AFTER INSERT OR UPDATE ON PSP_COMPANY_SERVICE
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_COMPANY_SERVICE_AT();


ALTER TABLE PSP_COMPANY_SERVICE DISABLE TRIGGER PSP_COMPANY_SERVICE_AT;
ALTER TABLE PSP_COMPANY_SERVICE ENABLE TRIGGER PSP_COMPANY_SERVICE_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_CONTACT_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT CREATOR_ID INTO v_creator_id FROM PSP_INDIVIDUAL WHERE INDIVIDUAL_SEQ = NEW.CONTACT_SEQ;
SELECT MODIFIER_ID INTO v_modifier_id FROM PSP_INDIVIDUAL WHERE INDIVIDUAL_SEQ = NEW.CONTACT_SEQ;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.AUTH_SIGNER_YN_IND <> OLD.AUTH_SIGNER_YN_IND) AND NEW.AUTH_SIGNER_YN_IND IS NOT NULL AND OLD.AUTH_SIGNER_YN_IND IS NOT NULL) OR
         (NEW.AUTH_SIGNER_YN_IND IS NULL AND OLD.AUTH_SIGNER_YN_IND IS NOT NULL) OR
         (NEW.AUTH_SIGNER_YN_IND IS NOT NULL AND OLD.AUTH_SIGNER_YN_IND IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Contact', 'AuthSignerYnInd', (OLD.AUTH_SIGNER_YN_IND )::VARCHAR, (NEW.AUTH_SIGNER_YN_IND )::VARCHAR, NEW.CONTACT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.CONTACT_ROLE_CD <> OLD.CONTACT_ROLE_CD) AND NEW.CONTACT_ROLE_CD IS NOT NULL AND OLD.CONTACT_ROLE_CD IS NOT NULL) OR
         (NEW.CONTACT_ROLE_CD IS NULL AND OLD.CONTACT_ROLE_CD IS NOT NULL) OR
         (NEW.CONTACT_ROLE_CD IS NOT NULL AND OLD.CONTACT_ROLE_CD IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Contact', 'ContactRoleCd', (OLD.CONTACT_ROLE_CD )::VARCHAR, (NEW.CONTACT_ROLE_CD )::VARCHAR, NEW.CONTACT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.FAX <> OLD.FAX) AND NEW.FAX IS NOT NULL AND OLD.FAX IS NOT NULL) OR
         (NEW.FAX IS NULL AND OLD.FAX IS NOT NULL) OR
         (NEW.FAX IS NOT NULL AND OLD.FAX IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Contact', 'Fax', (OLD.FAX )::VARCHAR, (NEW.FAX )::VARCHAR, NEW.CONTACT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.JOB_TITLE <> OLD.JOB_TITLE) AND NEW.JOB_TITLE IS NOT NULL AND OLD.JOB_TITLE IS NOT NULL) OR
         (NEW.JOB_TITLE IS NULL AND OLD.JOB_TITLE IS NOT NULL) OR
         (NEW.JOB_TITLE IS NOT NULL AND OLD.JOB_TITLE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Contact', 'JobTitle', (OLD.JOB_TITLE )::VARCHAR, (NEW.JOB_TITLE )::VARCHAR, NEW.CONTACT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.SOURCE_CONTACT_ID <> OLD.SOURCE_CONTACT_ID) AND NEW.SOURCE_CONTACT_ID IS NOT NULL AND OLD.SOURCE_CONTACT_ID IS NOT NULL) OR
         (NEW.SOURCE_CONTACT_ID IS NULL AND OLD.SOURCE_CONTACT_ID IS NOT NULL) OR
         (NEW.SOURCE_CONTACT_ID IS NOT NULL AND OLD.SOURCE_CONTACT_ID IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Contact', 'SourceContactId', (OLD.SOURCE_CONTACT_ID )::VARCHAR, (NEW.SOURCE_CONTACT_ID )::VARCHAR, NEW.CONTACT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TITLE <> OLD.TITLE) AND NEW.TITLE IS NOT NULL AND OLD.TITLE IS NOT NULL) OR
         (NEW.TITLE IS NULL AND OLD.TITLE IS NOT NULL) OR
         (NEW.TITLE IS NOT NULL AND OLD.TITLE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Contact', 'Title', (OLD.TITLE )::VARCHAR, (NEW.TITLE )::VARCHAR, NEW.CONTACT_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_CONTACT_AT
AFTER INSERT OR UPDATE ON PSP_CONTACT
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_CONTACT_AT();


ALTER TABLE PSP_CONTACT DISABLE TRIGGER PSP_CONTACT_AT;
ALTER TABLE PSP_CONTACT ENABLE TRIGGER PSP_CONTACT_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_DDCOMPANY_SERVICE_INFO_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT CREATOR_ID INTO v_creator_id FROM PSP_COMPANY_SERVICE WHERE COMPANY_SERVICE_SEQ = NEW.DDCOMPANY_SERVICE_INFO_SEQ;
SELECT MODIFIER_ID INTO v_modifier_id FROM PSP_COMPANY_SERVICE WHERE COMPANY_SERVICE_SEQ = NEW.DDCOMPANY_SERVICE_INFO_SEQ;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT <> OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT) AND NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NULL AND OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_SERVICE t1 WHERE t1.COMPANY_SERVICE_SEQ = NEW.DDCOMPANY_SERVICE_INFO_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'DDCompanyServiceInfo', 'OverrideCompanyLimitAmount', (OLD.OVERRIDE_COMPANY_LIMIT_AMOUNT )::VARCHAR, (NEW.OVERRIDE_COMPANY_LIMIT_AMOUNT )::VARCHAR, NEW.DDCOMPANY_SERVICE_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT <> OLD.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT) AND NEW.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT IS NULL AND OLD.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT IS NOT NULL) OR
         (NEW.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT IS NOT NULL AND OLD.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_COMPANY_SERVICE t1 WHERE t1.COMPANY_SERVICE_SEQ = NEW.DDCOMPANY_SERVICE_INFO_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'DDCompanyServiceInfo', 'OverrideEmployeeLimitAmount', (OLD.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT )::VARCHAR, (NEW.OVERRIDE_EMPLOYEE_LIMIT_AMOUNT )::VARCHAR, NEW.DDCOMPANY_SERVICE_INFO_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_DDCOMPANY_SERVICE_INFO_AT
AFTER INSERT OR UPDATE ON PSP_DDCOMPANY_SERVICE_INFO
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_DDCOMPANY_SERVICE_INFO_AT();


ALTER TABLE PSP_DDCOMPANY_SERVICE_INFO DISABLE TRIGGER PSP_DDCOMPANY_SERVICE_INFO_AT;
ALTER TABLE PSP_DDCOMPANY_SERVICE_INFO ENABLE TRIGGER PSP_DDCOMPANY_SERVICE_INFO_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_EMPLOYEE_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT CREATOR_ID INTO v_creator_id FROM PSP_INDIVIDUAL WHERE INDIVIDUAL_SEQ = NEW.EMPLOYEE_SEQ;
SELECT MODIFIER_ID INTO v_modifier_id FROM PSP_INDIVIDUAL WHERE INDIVIDUAL_SEQ = NEW.EMPLOYEE_SEQ;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.BIRTH_DATE_ENC <> OLD.BIRTH_DATE_ENC) AND NEW.BIRTH_DATE_ENC IS NOT NULL AND OLD.BIRTH_DATE_ENC IS NOT NULL) OR
         (NEW.BIRTH_DATE_ENC IS NULL AND OLD.BIRTH_DATE_ENC IS NOT NULL) OR
         (NEW.BIRTH_DATE_ENC IS NOT NULL AND OLD.BIRTH_DATE_ENC IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'BirthDateEnc', (OLD.BIRTH_DATE_ENC )::VARCHAR, (NEW.BIRTH_DATE_ENC )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.FED_ALLOWANCES <> OLD.FED_ALLOWANCES) AND NEW.FED_ALLOWANCES IS NOT NULL AND OLD.FED_ALLOWANCES IS NOT NULL) OR
         (NEW.FED_ALLOWANCES IS NULL AND OLD.FED_ALLOWANCES IS NOT NULL) OR
         (NEW.FED_ALLOWANCES IS NOT NULL AND OLD.FED_ALLOWANCES IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'FedAllowances', (OLD.FED_ALLOWANCES )::VARCHAR, (NEW.FED_ALLOWANCES )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.FED_FILING_STATUS <> OLD.FED_FILING_STATUS) AND NEW.FED_FILING_STATUS IS NOT NULL AND OLD.FED_FILING_STATUS IS NOT NULL) OR
         (NEW.FED_FILING_STATUS IS NULL AND OLD.FED_FILING_STATUS IS NOT NULL) OR
         (NEW.FED_FILING_STATUS IS NOT NULL AND OLD.FED_FILING_STATUS IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'FedFilingStatus', (OLD.FED_FILING_STATUS )::VARCHAR, (NEW.FED_FILING_STATUS )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.HIRE_DATE <> OLD.HIRE_DATE) AND NEW.HIRE_DATE IS NOT NULL AND OLD.HIRE_DATE IS NOT NULL) OR
         (NEW.HIRE_DATE IS NULL AND OLD.HIRE_DATE IS NOT NULL) OR
         (NEW.HIRE_DATE IS NOT NULL AND OLD.HIRE_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'HireDate', (OLD.HIRE_DATE )::VARCHAR, (NEW.HIRE_DATE )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.RE_HIRE_DATE <> OLD.RE_HIRE_DATE) AND NEW.RE_HIRE_DATE IS NOT NULL AND OLD.RE_HIRE_DATE IS NOT NULL) OR
         (NEW.RE_HIRE_DATE IS NULL AND OLD.RE_HIRE_DATE IS NOT NULL) OR
         (NEW.RE_HIRE_DATE IS NOT NULL AND OLD.RE_HIRE_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'ReHireDate', (OLD.RE_HIRE_DATE )::VARCHAR, (NEW.RE_HIRE_DATE )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TERMINATION_DATE <> OLD.TERMINATION_DATE) AND NEW.TERMINATION_DATE IS NOT NULL AND OLD.TERMINATION_DATE IS NOT NULL) OR
         (NEW.TERMINATION_DATE IS NULL AND OLD.TERMINATION_DATE IS NOT NULL) OR
         (NEW.TERMINATION_DATE IS NOT NULL AND OLD.TERMINATION_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'TerminationDate', (OLD.TERMINATION_DATE )::VARCHAR, (NEW.TERMINATION_DATE )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.WORK_STATE <> OLD.WORK_STATE) AND NEW.WORK_STATE IS NOT NULL AND OLD.WORK_STATE IS NOT NULL) OR
         (NEW.WORK_STATE IS NULL AND OLD.WORK_STATE IS NOT NULL) OR
         (NEW.WORK_STATE IS NOT NULL AND OLD.WORK_STATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'WorkState', (OLD.WORK_STATE )::VARCHAR, (NEW.WORK_STATE )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.LIVE_STATE <> OLD.LIVE_STATE) AND NEW.LIVE_STATE IS NOT NULL AND OLD.LIVE_STATE IS NOT NULL) OR
         (NEW.LIVE_STATE IS NULL AND OLD.LIVE_STATE IS NOT NULL) OR
         (NEW.LIVE_STATE IS NOT NULL AND OLD.LIVE_STATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'LiveState', (OLD.LIVE_STATE )::VARCHAR, (NEW.LIVE_STATE )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.SOURCE_EMPLOYEE_ID <> OLD.SOURCE_EMPLOYEE_ID) AND NEW.SOURCE_EMPLOYEE_ID IS NOT NULL AND OLD.SOURCE_EMPLOYEE_ID IS NOT NULL) OR
         (NEW.SOURCE_EMPLOYEE_ID IS NULL AND OLD.SOURCE_EMPLOYEE_ID IS NOT NULL) OR
         (NEW.SOURCE_EMPLOYEE_ID IS NOT NULL AND OLD.SOURCE_EMPLOYEE_ID IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'SourceEmployeeId', (OLD.SOURCE_EMPLOYEE_ID )::VARCHAR, (NEW.SOURCE_EMPLOYEE_ID )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.STATUS_CD <> OLD.STATUS_CD) AND NEW.STATUS_CD IS NOT NULL AND OLD.STATUS_CD IS NOT NULL) OR
         (NEW.STATUS_CD IS NULL AND OLD.STATUS_CD IS NOT NULL) OR
         (NEW.STATUS_CD IS NOT NULL AND OLD.STATUS_CD IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'StatusCd', (OLD.STATUS_CD )::VARCHAR, (NEW.STATUS_CD )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TAX_ID_ENC <> OLD.TAX_ID_ENC) AND NEW.TAX_ID_ENC IS NOT NULL AND OLD.TAX_ID_ENC IS NOT NULL) OR
         (NEW.TAX_ID_ENC IS NULL AND OLD.TAX_ID_ENC IS NOT NULL) OR
         (NEW.TAX_ID_ENC IS NOT NULL AND OLD.TAX_ID_ENC IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'TaxIdEnc', (OLD.TAX_ID_ENC )::VARCHAR, (NEW.TAX_ID_ENC )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TP_401K_INFO_IS_HCE <> OLD.TP_401K_INFO_IS_HCE) AND NEW.TP_401K_INFO_IS_HCE IS NOT NULL AND OLD.TP_401K_INFO_IS_HCE IS NOT NULL) OR
         (NEW.TP_401K_INFO_IS_HCE IS NULL AND OLD.TP_401K_INFO_IS_HCE IS NOT NULL) OR
         (NEW.TP_401K_INFO_IS_HCE IS NOT NULL AND OLD.TP_401K_INFO_IS_HCE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'ThirdParty401kInfo.IsHighlyCompensated', (OLD.TP_401K_INFO_IS_HCE )::VARCHAR, (NEW.TP_401K_INFO_IS_HCE )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TP_401K_INFO_OWNER_PERCENT <> OLD.TP_401K_INFO_OWNER_PERCENT) AND NEW.TP_401K_INFO_OWNER_PERCENT IS NOT NULL AND OLD.TP_401K_INFO_OWNER_PERCENT IS NOT NULL) OR
         (NEW.TP_401K_INFO_OWNER_PERCENT IS NULL AND OLD.TP_401K_INFO_OWNER_PERCENT IS NOT NULL) OR
         (NEW.TP_401K_INFO_OWNER_PERCENT IS NOT NULL AND OLD.TP_401K_INFO_OWNER_PERCENT IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'ThirdParty401kInfo.OwnershipPercentage', (OLD.TP_401K_INFO_OWNER_PERCENT )::VARCHAR, (NEW.TP_401K_INFO_OWNER_PERCENT )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TP_401K_INFO_IS_FAMILY_MEMBER <> OLD.TP_401K_INFO_IS_FAMILY_MEMBER) AND NEW.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL AND OLD.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL) OR
         (NEW.TP_401K_INFO_IS_FAMILY_MEMBER IS NULL AND OLD.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL) OR
         (NEW.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL AND OLD.TP_401K_INFO_IS_FAMILY_MEMBER IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'ThirdParty401kInfo.IsFamilyMember', (OLD.TP_401K_INFO_IS_FAMILY_MEMBER )::VARCHAR, (NEW.TP_401K_INFO_IS_FAMILY_MEMBER )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TP_401K_INFO_LAST_UPLOAD_DATE <> OLD.TP_401K_INFO_LAST_UPLOAD_DATE) AND NEW.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL AND OLD.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL) OR
         (NEW.TP_401K_INFO_LAST_UPLOAD_DATE IS NULL AND OLD.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL) OR
         (NEW.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL AND OLD.TP_401K_INFO_LAST_UPLOAD_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Employee', 'ThirdParty401kInfo.Last401kUploadDate', (OLD.TP_401K_INFO_LAST_UPLOAD_DATE )::VARCHAR, (NEW.TP_401K_INFO_LAST_UPLOAD_DATE )::VARCHAR, NEW.EMPLOYEE_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_EMPLOYEE_AT
AFTER INSERT OR UPDATE ON PSP_EMPLOYEE
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_EMPLOYEE_AT();


ALTER TABLE PSP_EMPLOYEE DISABLE TRIGGER PSP_EMPLOYEE_AT;
ALTER TABLE PSP_EMPLOYEE ENABLE TRIGGER PSP_EMPLOYEE_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_INDIVIDUAL_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.FIRST_NAME <> OLD.FIRST_NAME) AND NEW.FIRST_NAME IS NOT NULL AND OLD.FIRST_NAME IS NOT NULL) OR
         (NEW.FIRST_NAME IS NULL AND OLD.FIRST_NAME IS NOT NULL) OR
         (NEW.FIRST_NAME IS NOT NULL AND OLD.FIRST_NAME IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_EMPLOYEE t1 WHERE t1.EMPLOYEE_SEQ = NEW.INDIVIDUAL_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Individual', 'FirstName', (OLD.FIRST_NAME )::VARCHAR, (NEW.FIRST_NAME )::VARCHAR, NEW.INDIVIDUAL_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.MIDDLE_NAME <> OLD.MIDDLE_NAME) AND NEW.MIDDLE_NAME IS NOT NULL AND OLD.MIDDLE_NAME IS NOT NULL) OR
         (NEW.MIDDLE_NAME IS NULL AND OLD.MIDDLE_NAME IS NOT NULL) OR
         (NEW.MIDDLE_NAME IS NOT NULL AND OLD.MIDDLE_NAME IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_EMPLOYEE t1 WHERE t1.EMPLOYEE_SEQ = NEW.INDIVIDUAL_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Individual', 'MiddleName', (OLD.MIDDLE_NAME )::VARCHAR, (NEW.MIDDLE_NAME )::VARCHAR, NEW.INDIVIDUAL_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.LAST_NAME <> OLD.LAST_NAME) AND NEW.LAST_NAME IS NOT NULL AND OLD.LAST_NAME IS NOT NULL) OR
         (NEW.LAST_NAME IS NULL AND OLD.LAST_NAME IS NOT NULL) OR
         (NEW.LAST_NAME IS NOT NULL AND OLD.LAST_NAME IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_EMPLOYEE t1 WHERE t1.EMPLOYEE_SEQ = NEW.INDIVIDUAL_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Individual', 'LastName', (OLD.LAST_NAME )::VARCHAR, (NEW.LAST_NAME )::VARCHAR, NEW.INDIVIDUAL_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.PHONE <> OLD.PHONE) AND NEW.PHONE IS NOT NULL AND OLD.PHONE IS NOT NULL) OR
         (NEW.PHONE IS NULL AND OLD.PHONE IS NOT NULL) OR
         (NEW.PHONE IS NOT NULL AND OLD.PHONE IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_EMPLOYEE t1 WHERE t1.EMPLOYEE_SEQ = NEW.INDIVIDUAL_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Individual', 'Phone', (OLD.PHONE )::VARCHAR, (NEW.PHONE )::VARCHAR, NEW.INDIVIDUAL_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.EMAIL <> OLD.EMAIL) AND NEW.EMAIL IS NOT NULL AND OLD.EMAIL IS NOT NULL) OR
         (NEW.EMAIL IS NULL AND OLD.EMAIL IS NOT NULL) OR
         (NEW.EMAIL IS NOT NULL AND OLD.EMAIL IS NULL) ) THEN
         BEGIN
            SELECT t1.COMPANY_FK INTO v_company_fk from PSP_EMPLOYEE t1 WHERE t1.EMPLOYEE_SEQ = NEW.INDIVIDUAL_SEQ;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'Individual', 'Email', (OLD.EMAIL )::VARCHAR, (NEW.EMAIL )::VARCHAR, NEW.INDIVIDUAL_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_INDIVIDUAL_AT
AFTER INSERT OR UPDATE ON PSP_INDIVIDUAL
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_INDIVIDUAL_AT();


ALTER TABLE PSP_INDIVIDUAL DISABLE TRIGGER PSP_INDIVIDUAL_AT;
ALTER TABLE PSP_INDIVIDUAL ENABLE TRIGGER PSP_INDIVIDUAL_AT;

;
CREATE OR REPLACE FUNCTION FN_PSP_MONEY_MVMT_TRANS_AT()
RETURNS TRIGGER
AS $$
DECLARE
  v_company_fk VARCHAR(255);
  v_creator_id VARCHAR(30);
  v_modifier_id VARCHAR(30);
  v_psp_utc_systimestamp TIMESTAMP;
BEGIN
SELECT NEW.CREATOR_ID INTO v_creator_id FROM DUAL;
SELECT NEW.MODIFIER_ID INTO v_modifier_id FROM DUAL;

  IF (TG_OP='UPDATE') THEN
     IF (((NEW.AGENCY_TAXPAYER_ID_ENC <> OLD.AGENCY_TAXPAYER_ID_ENC) AND NEW.AGENCY_TAXPAYER_ID_ENC IS NOT NULL AND OLD.AGENCY_TAXPAYER_ID_ENC IS NOT NULL) OR
         (NEW.AGENCY_TAXPAYER_ID_ENC IS NULL AND OLD.AGENCY_TAXPAYER_ID_ENC IS NOT NULL) OR
         (NEW.AGENCY_TAXPAYER_ID_ENC IS NOT NULL AND OLD.AGENCY_TAXPAYER_ID_ENC IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'MoneyMovementTransaction', 'AgencyTaxpayerIdEnc', (OLD.AGENCY_TAXPAYER_ID_ENC )::VARCHAR, (NEW.AGENCY_TAXPAYER_ID_ENC )::VARCHAR, NEW.MONEY_MOVEMENT_TRANSACTION_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.INITIATION_DATE <> OLD.INITIATION_DATE) AND NEW.INITIATION_DATE IS NOT NULL AND OLD.INITIATION_DATE IS NOT NULL) OR
         (NEW.INITIATION_DATE IS NULL AND OLD.INITIATION_DATE IS NOT NULL) OR
         (NEW.INITIATION_DATE IS NOT NULL AND OLD.INITIATION_DATE IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'MoneyMovementTransaction', 'InitiationDate', (OLD.INITIATION_DATE )::VARCHAR, (NEW.INITIATION_DATE )::VARCHAR, NEW.MONEY_MOVEMENT_TRANSACTION_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.MANUAL_PAYMENT_STATUS <> OLD.MANUAL_PAYMENT_STATUS) AND NEW.MANUAL_PAYMENT_STATUS IS NOT NULL AND OLD.MANUAL_PAYMENT_STATUS IS NOT NULL) OR
         (NEW.MANUAL_PAYMENT_STATUS IS NULL AND OLD.MANUAL_PAYMENT_STATUS IS NOT NULL) OR
         (NEW.MANUAL_PAYMENT_STATUS IS NOT NULL AND OLD.MANUAL_PAYMENT_STATUS IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'MoneyMovementTransaction', 'ManualPaymentStatus', (OLD.MANUAL_PAYMENT_STATUS )::VARCHAR, (NEW.MANUAL_PAYMENT_STATUS )::VARCHAR, NEW.MONEY_MOVEMENT_TRANSACTION_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.MONEY_MOVEMENT_PAYMENT_METHOD <> OLD.MONEY_MOVEMENT_PAYMENT_METHOD) AND NEW.MONEY_MOVEMENT_PAYMENT_METHOD IS NOT NULL AND OLD.MONEY_MOVEMENT_PAYMENT_METHOD IS NOT NULL) OR
         (NEW.MONEY_MOVEMENT_PAYMENT_METHOD IS NULL AND OLD.MONEY_MOVEMENT_PAYMENT_METHOD IS NOT NULL) OR
         (NEW.MONEY_MOVEMENT_PAYMENT_METHOD IS NOT NULL AND OLD.MONEY_MOVEMENT_PAYMENT_METHOD IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'MoneyMovementTransaction', 'MoneyMovementPaymentMethod', (OLD.MONEY_MOVEMENT_PAYMENT_METHOD )::VARCHAR, (NEW.MONEY_MOVEMENT_PAYMENT_METHOD )::VARCHAR, NEW.MONEY_MOVEMENT_TRANSACTION_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.TAX_PAYMENT_STATUS <> OLD.TAX_PAYMENT_STATUS) AND NEW.TAX_PAYMENT_STATUS IS NOT NULL AND OLD.TAX_PAYMENT_STATUS IS NOT NULL) OR
         (NEW.TAX_PAYMENT_STATUS IS NULL AND OLD.TAX_PAYMENT_STATUS IS NOT NULL) OR
         (NEW.TAX_PAYMENT_STATUS IS NOT NULL AND OLD.TAX_PAYMENT_STATUS IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'MoneyMovementTransaction', 'TaxPaymentStatus', (OLD.TAX_PAYMENT_STATUS )::VARCHAR, (NEW.TAX_PAYMENT_STATUS )::VARCHAR, NEW.MONEY_MOVEMENT_TRANSACTION_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

     IF (((NEW.REFERENCE_NUMBER <> OLD.REFERENCE_NUMBER) AND NEW.REFERENCE_NUMBER IS NOT NULL AND OLD.REFERENCE_NUMBER IS NOT NULL) OR
         (NEW.REFERENCE_NUMBER IS NULL AND OLD.REFERENCE_NUMBER IS NOT NULL) OR
         (NEW.REFERENCE_NUMBER IS NOT NULL AND OLD.REFERENCE_NUMBER IS NULL) ) THEN
         BEGIN
            SELECT NEW.COMPANY_FK INTO v_company_fk from DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            SELECT CURRENT_TIMESTAMP AT TIME ZONE 'UTC' +(SELECT INTERVAL '1 second'*(SELECT (SYSTEM_PARAMETER_VALUE)::DOUBLE PRECISION/1000 FROM PSP_SYSTEM_PARAMETER WHERE SYSTEM_PARAMETER_CD = 'PSP_DATE_OFFSET')) INTO v_psp_utc_systimestamp FROM DUAL;
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (gen_random_uuid(), 0, v_psp_utc_systimestamp, v_psp_utc_systimestamp, 'MoneyMovementTransaction', 'ReferenceNumber', (OLD.REFERENCE_NUMBER )::VARCHAR, (NEW.REFERENCE_NUMBER )::VARCHAR, NEW.MONEY_MOVEMENT_TRANSACTION_SEQ, v_modifier_id, v_psp_utc_systimestamp, v_company_fk);
         END IF;
     END IF;

  END IF;
  RETURN NEW;
END;
$$
LANGUAGE PLPGSQL;

CREATE TRIGGER PSP_MONEY_MVMT_TRANS_AT
AFTER INSERT OR UPDATE ON PSP_MONEY_MOVEMENT_TRANSACTION
FOR EACH ROW
EXECUTE FUNCTION FN_PSP_MONEY_MVMT_TRANS_AT();


ALTER TABLE PSP_MONEY_MOVEMENT_TRANSACTION DISABLE TRIGGER PSP_MONEY_MVMT_TRANS_AT;
ALTER TABLE PSP_MONEY_MOVEMENT_TRANSACTION ENABLE TRIGGER PSP_MONEY_MVMT_TRANS_AT;

;