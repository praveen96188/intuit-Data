<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="info">

    <Properties>
        <Property name="layout">%d{yyyy-MM-dd'T'HH:mm:ss,SSSZ} %5p %c{1}:%L - %m%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${layout}"/>
        </Console>
    </Appenders>

    <Loggers>
        <Root level="info" includeLocation="true">
            <AppenderRef ref="Console"/>
        </Root>

        <Logger name="com.intuit.identity.exptplatform" level="error"/>

        <!--Hibernate package log level-->
        <Logger name="org.hibernate" level="info"/>
        <!-- log just the SQL -->
        <Logger name="org.hibernate.SQL" level="debug"/>
        <!-- log JDBC bind parameters -->
        <Logger name="org.hibernate.type" level="trace"/>
        <!-- log schema export/update -->
        <Logger name="org.hibernate.tool.hbm2ddl" level="info"/>
        <!-- log cache activity -->
        <Logger name="org.hibernate.cache" level="info"/>
        <!-- log HQL parse trees -->
        <!--<Logger name="org.hibernate.hql" level="debug"/>-->
        <!-- log transaction activity -->
        <!--<Logger name="org.hibernate.transaction" level="debug"/>-->
        <!-- log JDBC resource acquisition -->
        <!--<Logger name="org.hibernate.jdbc" level="debug"/>-->

        <!-- enable the following line if you want to track down connection -->
        <!-- leakages when using DriverManagerConnectionProvider -->
        <!--<Logger name="org.hibernate.connection.DriverManagerConnectionProvider" level="trace"/>-->

    </Loggers>
</Configuration>