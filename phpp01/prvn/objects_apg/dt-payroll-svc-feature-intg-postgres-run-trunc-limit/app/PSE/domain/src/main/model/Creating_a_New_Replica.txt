1. Force sync PSP.eap
2. Remove read-only flag on PSP.eap
3. Tools --> Manage .EAP File --> Create new replica
4. Save the replica as a new name in the same directory as the model
5. Close PSP.eap
6. Open the replica