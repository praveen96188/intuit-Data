--
-- This script will be executed AFTER the automatically generated
-- D:\dev\PSP\rel-1.9\PSE\Domain\src\main\model\DBUpgrade_002.000.000.062.sql
--
-- Developers can hand code logic here for data migration purposes
--

CREATE OR REPLACE TRIGGER PSPADM.PSP_EMPLOYEE_AT
AFTER INSERT OR UPDATE ON PSP_EMPLOYEE
FOR EACH ROW
DECLARE
  v_company_fk VARCHAR2(255);
  v_creator_id VARCHAR2(30);
  v_modifier_id VARCHAR2(30);
BEGIN

SELECT CREATOR_ID INTO v_creator_id FROM PSP_INDIVIDUAL WHERE INDIVIDUAL_SEQ = :NEW.EMPLOYEE_SEQ;
SELECT MODIFIER_ID INTO v_modifier_id FROM PSP_INDIVIDUAL WHERE INDIVIDUAL_SEQ = :NEW.EMPLOYEE_SEQ;

  IF (UPDATING) THEN
     IF (((:NEW.BIRTH_DATE <> :OLD.BIRTH_DATE) AND :NEW.BIRTH_DATE IS NOT NULL AND :OLD.BIRTH_DATE IS NOT NULL) OR
         (:NEW.BIRTH_DATE IS NULL AND :OLD.BIRTH_DATE IS NOT NULL) OR
         (:NEW.BIRTH_DATE IS NOT NULL AND :OLD.BIRTH_DATE IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'BirthDate', TO_CHAR(:OLD.BIRTH_DATE ), TO_CHAR(:NEW.BIRTH_DATE ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.FED_ALLOWANCES <> :OLD.FED_ALLOWANCES) AND :NEW.FED_ALLOWANCES IS NOT NULL AND :OLD.FED_ALLOWANCES IS NOT NULL) OR
         (:NEW.FED_ALLOWANCES IS NULL AND :OLD.FED_ALLOWANCES IS NOT NULL) OR
         (:NEW.FED_ALLOWANCES IS NOT NULL AND :OLD.FED_ALLOWANCES IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'FedAllowances', TO_CHAR(:OLD.FED_ALLOWANCES ), TO_CHAR(:NEW.FED_ALLOWANCES ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.FED_FILING_STATUS <> :OLD.FED_FILING_STATUS) AND :NEW.FED_FILING_STATUS IS NOT NULL AND :OLD.FED_FILING_STATUS IS NOT NULL) OR
         (:NEW.FED_FILING_STATUS IS NULL AND :OLD.FED_FILING_STATUS IS NOT NULL) OR
         (:NEW.FED_FILING_STATUS IS NOT NULL AND :OLD.FED_FILING_STATUS IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'FedFilingStatus', TO_CHAR(:OLD.FED_FILING_STATUS ), TO_CHAR(:NEW.FED_FILING_STATUS ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.HIRE_DATE <> :OLD.HIRE_DATE) AND :NEW.HIRE_DATE IS NOT NULL AND :OLD.HIRE_DATE IS NOT NULL) OR
         (:NEW.HIRE_DATE IS NULL AND :OLD.HIRE_DATE IS NOT NULL) OR
         (:NEW.HIRE_DATE IS NOT NULL AND :OLD.HIRE_DATE IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'HireDate', TO_CHAR(:OLD.HIRE_DATE ), TO_CHAR(:NEW.HIRE_DATE ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.RE_HIRE_DATE <> :OLD.RE_HIRE_DATE) AND :NEW.RE_HIRE_DATE IS NOT NULL AND :OLD.RE_HIRE_DATE IS NOT NULL) OR
         (:NEW.RE_HIRE_DATE IS NULL AND :OLD.RE_HIRE_DATE IS NOT NULL) OR
         (:NEW.RE_HIRE_DATE IS NOT NULL AND :OLD.RE_HIRE_DATE IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'ReHireDate', TO_CHAR(:OLD.RE_HIRE_DATE ), TO_CHAR(:NEW.RE_HIRE_DATE ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.TERMINATION_DATE <> :OLD.TERMINATION_DATE) AND :NEW.TERMINATION_DATE IS NOT NULL AND :OLD.TERMINATION_DATE IS NOT NULL) OR
         (:NEW.TERMINATION_DATE IS NULL AND :OLD.TERMINATION_DATE IS NOT NULL) OR
         (:NEW.TERMINATION_DATE IS NOT NULL AND :OLD.TERMINATION_DATE IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'TerminationDate', TO_CHAR(:OLD.TERMINATION_DATE ), TO_CHAR(:NEW.TERMINATION_DATE ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.WORK_STATE <> :OLD.WORK_STATE) AND :NEW.WORK_STATE IS NOT NULL AND :OLD.WORK_STATE IS NOT NULL) OR
         (:NEW.WORK_STATE IS NULL AND :OLD.WORK_STATE IS NOT NULL) OR
         (:NEW.WORK_STATE IS NOT NULL AND :OLD.WORK_STATE IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'WorkState', TO_CHAR(:OLD.WORK_STATE ), TO_CHAR(:NEW.WORK_STATE ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.SOURCE_EMPLOYEE_ID <> :OLD.SOURCE_EMPLOYEE_ID) AND :NEW.SOURCE_EMPLOYEE_ID IS NOT NULL AND :OLD.SOURCE_EMPLOYEE_ID IS NOT NULL) OR
         (:NEW.SOURCE_EMPLOYEE_ID IS NULL AND :OLD.SOURCE_EMPLOYEE_ID IS NOT NULL) OR
         (:NEW.SOURCE_EMPLOYEE_ID IS NOT NULL AND :OLD.SOURCE_EMPLOYEE_ID IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'SourceEmployeeId', TO_CHAR(:OLD.SOURCE_EMPLOYEE_ID ), TO_CHAR(:NEW.SOURCE_EMPLOYEE_ID ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.STATUS_CD <> :OLD.STATUS_CD) AND :NEW.STATUS_CD IS NOT NULL AND :OLD.STATUS_CD IS NOT NULL) OR
         (:NEW.STATUS_CD IS NULL AND :OLD.STATUS_CD IS NOT NULL) OR
         (:NEW.STATUS_CD IS NOT NULL AND :OLD.STATUS_CD IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'StatusCd', TO_CHAR(:OLD.STATUS_CD ), TO_CHAR(:NEW.STATUS_CD ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.TAX_ID <> :OLD.TAX_ID) AND :NEW.TAX_ID IS NOT NULL AND :OLD.TAX_ID IS NOT NULL) OR
         (:NEW.TAX_ID IS NULL AND :OLD.TAX_ID IS NOT NULL) OR
         (:NEW.TAX_ID IS NOT NULL AND :OLD.TAX_ID IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'TaxId', TO_CHAR(:OLD.TAX_ID ), TO_CHAR(:NEW.TAX_ID ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.TP_401K_INFO_IS_HCE <> :OLD.TP_401K_INFO_IS_HCE) AND :NEW.TP_401K_INFO_IS_HCE IS NOT NULL AND :OLD.TP_401K_INFO_IS_HCE IS NOT NULL) OR
         (:NEW.TP_401K_INFO_IS_HCE IS NULL AND :OLD.TP_401K_INFO_IS_HCE IS NOT NULL) OR
         (:NEW.TP_401K_INFO_IS_HCE IS NOT NULL AND :OLD.TP_401K_INFO_IS_HCE IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'ThirdParty401kInfo.IsHighlyCompensated', TO_CHAR(:OLD.TP_401K_INFO_IS_HCE ), TO_CHAR(:NEW.TP_401K_INFO_IS_HCE ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.TP_401K_INFO_OWNER_PERCENT <> :OLD.TP_401K_INFO_OWNER_PERCENT) AND :NEW.TP_401K_INFO_OWNER_PERCENT IS NOT NULL AND :OLD.TP_401K_INFO_OWNER_PERCENT IS NOT NULL) OR
         (:NEW.TP_401K_INFO_OWNER_PERCENT IS NULL AND :OLD.TP_401K_INFO_OWNER_PERCENT IS NOT NULL) OR
         (:NEW.TP_401K_INFO_OWNER_PERCENT IS NOT NULL AND :OLD.TP_401K_INFO_OWNER_PERCENT IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'ThirdParty401kInfo.OwnershipPercentage', TO_CHAR(:OLD.TP_401K_INFO_OWNER_PERCENT ), TO_CHAR(:NEW.TP_401K_INFO_OWNER_PERCENT ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.TP_401K_INFO_IS_FAMILY_MEMBER <> :OLD.TP_401K_INFO_IS_FAMILY_MEMBER) AND :NEW.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL AND :OLD.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL) OR
         (:NEW.TP_401K_INFO_IS_FAMILY_MEMBER IS NULL AND :OLD.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL) OR
         (:NEW.TP_401K_INFO_IS_FAMILY_MEMBER IS NOT NULL AND :OLD.TP_401K_INFO_IS_FAMILY_MEMBER IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'ThirdParty401kInfo.IsFamilyMember', TO_CHAR(:OLD.TP_401K_INFO_IS_FAMILY_MEMBER ), TO_CHAR(:NEW.TP_401K_INFO_IS_FAMILY_MEMBER ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

     IF (((:NEW.TP_401K_INFO_LAST_UPLOAD_DATE <> :OLD.TP_401K_INFO_LAST_UPLOAD_DATE) AND :NEW.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL AND :OLD.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL) OR
         (:NEW.TP_401K_INFO_LAST_UPLOAD_DATE IS NULL AND :OLD.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL) OR
         (:NEW.TP_401K_INFO_LAST_UPLOAD_DATE IS NOT NULL AND :OLD.TP_401K_INFO_LAST_UPLOAD_DATE IS NULL) ) THEN
         BEGIN
            SELECT :NEW.COMPANY_FK INTO v_company_fk FROM DUAL;
         EXCEPTION
            WHEN NO_DATA_FOUND THEN v_company_fk := NULL;
            WHEN OTHERS THEN RAISE;
         END;

         IF v_company_fk IS NOT NULL THEN
            INSERT INTO PSP_PROPERTY_AUDIT
              (PROPERTY_AUDIT_SEQ, VERSION, CREATED_DATE, MODIFIED_DATE, CLASS_NAME, PROPERTY_NAME, OLD_PROPERTY_VALUE, NEW_PROPERTY_VALUE, OBJECT_IDENTIFIER, USER_ID, AUDIT_DATE, COMPANY_FK)
            VALUES
              (SYS_GUID(), 0, SYS_EXTRACT_UTC(SYSTIMESTAMP), SYS_EXTRACT_UTC(SYSTIMESTAMP), 'Employee', 'ThirdParty401kInfo.Last401kUploadDate', TO_CHAR(:OLD.TP_401K_INFO_LAST_UPLOAD_DATE ), TO_CHAR(:NEW.TP_401K_INFO_LAST_UPLOAD_DATE ), :NEW.EMPLOYEE_SEQ, v_modifier_id, SYS_EXTRACT_UTC(SYSTIMESTAMP), v_company_fk);
         END IF;
     END IF;

  END IF;
END;
/


