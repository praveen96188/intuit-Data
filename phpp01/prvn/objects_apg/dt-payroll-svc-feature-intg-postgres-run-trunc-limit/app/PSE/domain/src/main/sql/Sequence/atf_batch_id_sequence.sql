/
DECLARE
   sequence_exists PLS_INTEGER;
BEGIN
   SELECT COUNT(*) INTO sequence_exists
   FROM "USER_SEQUENCES"
   WHERE SEQUENCE_NAME = 'SEQ_ATF_BATCH_ID_NBR';
   IF sequence_exists = 0 THEN
    EXECUTE IMMEDIATE 'CREATE SEQUENCE SEQ_ATF_BATCH_ID_NBR INCREMENT BY 1 MINVALUE 10000 NOCYCLE';
   END IF;
end;
/
