--------------------------------------------------------------------------
-- Play this script in PREVIOUS_PSP_LOCAL@XE to make it look like PSP_LOCAL@XE
--
-- Please review the script before using it to make sure it won't
-- cause any unacceptable data loss.
--
-- PREVIOUS_PSP_LOCAL@XE Schema Extracted by User PREVIOUS_PSP_LOCAL 
-- PSP_LOCAL@XE Schema Extracted by User PSP_LOCAL 

Prompt Table PSP_WC_COMPANY;
CREATE TABLE PSP_WC_COMPANY
(
  WC_COMPANY_SEQ  VARCHAR2(255 CHAR)            NOT NULL,
  VERSION         NUMBER(19)                    NOT NULL,
  CREATOR_ID      VARCHAR2(30 CHAR),
  CREATED_DATE    TIMESTAMP(6)                  NOT NULL,
  MODIFIER_ID     VARCHAR2(30 CHAR),
  MODIFIED_DATE   TIMESTAMP(6)                  NOT NULL,
  REALM_ID        NUMBER(19)                    DEFAULT -1                    NOT NULL,
  SUBS_TYPE_CD    VARCHAR2(255 CHAR),
  COMPANY_FK      VARCHAR2(255 CHAR)
)
NOPARALLEL;

Prompt Index PSP_WC_COMPANY_FK1;
CREATE INDEX PSP_WC_COMPANY_FK1 ON PSP_WC_COMPANY
(COMPANY_FK, REALM_ID)
NOPARALLEL;

ALTER TABLE PSP_WC_COMPANY
 ADD CONSTRAINT C_PSP_WC_COMPANY0
  CHECK (SUBS_TYPE_CD IN('Next', 'SplitLimit'));

ALTER TABLE PSP_WC_COMPANY
 ADD PRIMARY KEY
  (WC_COMPANY_SEQ, REALM_ID)
  USING INDEX;

ALTER TABLE PSP_WC_COMPANY
 ADD CONSTRAINT PSP_WC_COMPANY_FK1 
  FOREIGN KEY (COMPANY_FK, REALM_ID) 
  REFERENCES PSP_COMPANY (COMPANY_SEQ,REALM_ID);

PROMPT finished DBUpgrade_002.022.008.004.sql