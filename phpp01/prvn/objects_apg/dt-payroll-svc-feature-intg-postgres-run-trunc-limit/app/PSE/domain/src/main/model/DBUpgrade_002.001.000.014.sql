--------------------------------------------------------------------------
-- Play this script in PREVIOUS_PSP_LOCAL@XE to make it look like PSP_LOCAL@XE
--
-- Please review the script before using it to make sure it won't
-- cause any unacceptable data loss.
--
-- PREVIOUS_PSP_LOCAL@XE Schema Extracted by User PREVIOUS_PSP_LOCAL 
-- PSP_LOCAL@XE Schema Extracted by User PSP_LOCAL 

Prompt Column EDITABLE_RATES;
ALTER TABLE PSP_LAW
 ADD (EDITABLE_RATES  NUMBER(1));

Prompt Table PSP_LAW_FILING_AMOUNT;
CREATE TABLE PSP_LAW_FILING_AMOUNT
(
  NAME          VARCHAR2(255 CHAR),
  VERSION       NUMBER(19),
  REALM_ID      NUMBER(19)                      DEFAULT -1,
  A_T_F_LAW_ID  VARCHAR2(30 CHAR),
  DESCRIPTION   VARCHAR2(255 CHAR),
  RATE          NUMBER(1),
  LAW_FK        VARCHAR2(255 CHAR)
)
NOPARALLEL;

Prompt Index PSP_LAW_FILING_AMOUNT_FK1;
CREATE INDEX PSP_LAW_FILING_AMOUNT_FK1 ON PSP_LAW_FILING_AMOUNT
(LAW_FK, REALM_ID)
NOPARALLEL;

ALTER TABLE PSP_LAW_FILING_AMOUNT
 ADD PRIMARY KEY
  (NAME, REALM_ID)
  USING INDEX;

Prompt Column LAST_QUARTER_TO_FILE;
ALTER TABLE PSP_TAX_COMPANY_SERVICE_INFO
 ADD (LAST_QUARTER_TO_FILE  NUMBER(10));

Prompt Column FILE_ANNUAL_RETURNS;
ALTER TABLE PSP_TAX_COMPANY_SERVICE_INFO
 ADD (FILE_ANNUAL_RETURNS  NUMBER(1));

Prompt Column FINAL_ANNUAL_RETURNS;
ALTER TABLE PSP_TAX_COMPANY_SERVICE_INFO
 ADD (FINAL_ANNUAL_RETURNS  NUMBER(1));

Prompt Column LAST_PAYROLL_DATE;
ALTER TABLE PSP_TAX_COMPANY_SERVICE_INFO
 ADD (LAST_PAYROLL_DATE  TIMESTAMP(6));

Prompt Column LAST_TAX_QUARTER;
ALTER TABLE PSP_TAX_COMPANY_SERVICE_INFO DROP COLUMN LAST_TAX_QUARTER;

Prompt Table PSP_COMPANY_LAW_FILING_AMOUNT;
CREATE TABLE PSP_COMPANY_LAW_FILING_AMOUNT
(
  COMPANY_LAW_FILING_AMOUNT_SEQ  VARCHAR2(255 CHAR),
  VERSION                        NUMBER(19),
  CREATOR_ID                     VARCHAR2(30 CHAR),
  CREATED_DATE                   TIMESTAMP(6),
  MODIFIER_ID                    VARCHAR2(30 CHAR),
  MODIFIED_DATE                  TIMESTAMP(6),
  REALM_ID                       NUMBER(19)     DEFAULT -1,
  AMOUNT                         NUMBER(19,7),
  EFFECTIVE_DATE                 TIMESTAMP(6),
  INVALID_DATE                   TIMESTAMP(6),
  NAME                           VARCHAR2(255 CHAR),
  COMPANY_LAW_FK                 VARCHAR2(255 CHAR)
)
NOPARALLEL;

Prompt Index PSP_COMPANYLAWFILINGAMOUNT_FK1;
CREATE INDEX PSP_COMPANYLAWFILINGAMOUNT_FK1 ON PSP_COMPANY_LAW_FILING_AMOUNT
(COMPANY_LAW_FK, REALM_ID)
NOPARALLEL;

ALTER TABLE PSP_COMPANY_LAW_FILING_AMOUNT
 ADD PRIMARY KEY
  (COMPANY_LAW_FILING_AMOUNT_SEQ, REALM_ID)
  USING INDEX;

ALTER TABLE PSP_LAW_FILING_AMOUNT
 ADD CONSTRAINT PSP_LAW_FILING_AMOUNT_FK1 
  FOREIGN KEY (LAW_FK, REALM_ID) 
  REFERENCES PSP_LAW (LAW_ID,REALM_ID);

ALTER TABLE PSP_COMPANY_LAW_FILING_AMOUNT
 ADD CONSTRAINT PSP_COMPANYLAWFILINGAMOUNT_FK1 
  FOREIGN KEY (COMPANY_LAW_FK, REALM_ID) 
  REFERENCES PSP_COMPANY_LAW (COMPANY_LAW_SEQ,REALM_ID);

PROMPT finishedDBUpgrade_002.001.000.014.sql