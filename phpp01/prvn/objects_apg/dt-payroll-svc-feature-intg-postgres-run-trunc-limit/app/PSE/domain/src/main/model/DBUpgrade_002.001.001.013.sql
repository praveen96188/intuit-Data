--------------------------------------------------------------------------
-- Play this script in PREVIOUS_PSP_LOCAL@XE to make it look like PSP_LOCAL@XE
--
-- Please review the script before using it to make sure it won't
-- cause any unacceptable data loss.
--
-- PREVIOUS_PSP_LOCAL@XE Schema Extracted by User PREVIOUS_PSP_LOCAL 
-- PSP_LOCAL@XE Schema Extracted by User PSP_LOCAL 

Prompt Column HOURLY_RATE;
ALTER TABLE PSP_EMPLOYEE_LAW_QTR_TOTALS
 ADD (HOURLY_RATE  NUMBER(19,7));

Prompt Table PSP_HOURS_WORKED_EXCEPTION;
CREATE TABLE PSP_HOURS_WORKED_EXCEPTION
(
  HOURS_WORKED_EXCEPTION_ID  VARCHAR2(255 CHAR),
  VERSION                    NUMBER(19),
  REALM_ID                   NUMBER(19)         DEFAULT -1,
  PAY_TYPE                   VARCHAR2(255 CHAR),
  PAYROLL_ITEM_CD            VARCHAR2(255 CHAR),
  LAW_FK                     VARCHAR2(255 CHAR)
)
NOPARALLEL;

Prompt Index PSP_HOURS_WORKED_EXCEPTION_FK1;
CREATE INDEX PSP_HOURS_WORKED_EXCEPTION_FK1 ON PSP_HOURS_WORKED_EXCEPTION
(LAW_FK, REALM_ID)
NOPARALLEL;

ALTER TABLE PSP_HOURS_WORKED_EXCEPTION
 ADD PRIMARY KEY
  (HOURS_WORKED_EXCEPTION_ID, REALM_ID)
  USING INDEX;

ALTER TABLE PSP_HOURS_WORKED_EXCEPTION
 ADD CONSTRAINT PSP_HOURS_WORKED_EXCEPTION_FK1 
  FOREIGN KEY (LAW_FK, REALM_ID) 
  REFERENCES PSP_LAW (LAW_ID,REALM_ID);

PROMPT finishedDBUpgrade_002.001.001.013.sql