--------------------------------------------------------------------------
-- Play this script in PREVIOUS_PSP_LOCAL@XE to make it look like PSP_LOCAL@XE
--
-- Please review the script before using it to make sure it won't
-- cause any unacceptable data loss.
--
-- PREVIOUS_PSP_LOCAL@XE Schema Extracted by User PREVIOUS_PSP_LOCAL 
-- PSP_LOCAL@XE Schema Extracted by User PSP_LOCAL 

Prompt Column VERSION;
ALTER TABLE PSP_AUTH_USER
MODIFY(VERSION  DEFAULT 0);

Prompt Column VERSION;
ALTER TABLE PSP_COMPANY
MODIFY(VERSION  DEFAULT 0);

Prompt Table PSP_ATFPAYMENTS_TO_PROCESS;
CREATE TABLE PSP_ATFPAYMENTS_TO_PROCESS
(
  ATFPAYMENTS_TO_PROCESS_SEQ     VARCHAR2(255 CHAR),
  VERSION                        NUMBER(19),
  CREATOR_ID                     VARCHAR2(30 CHAR),
  CREATED_DATE                   TIMESTAMP(6),
  MODIFIER_ID                    VARCHAR2(30 CHAR),
  MODIFIED_DATE                  TIMESTAMP(6),
  REALM_ID                       NUMBER(19)     DEFAULT -1,
  AMOUNT                         NUMBER(19,4),
  PAYMENT_DATE                   TIMESTAMP(6),
  QUARTER_END_DATE               TIMESTAMP(6),
  MONEY_MOVEMENT_TRANSACTION_FK  VARCHAR2(255 CHAR),
  LAW_FK                         VARCHAR2(255 CHAR),
  COMPANY_FK                     VARCHAR2(255 CHAR)
)
NOPARALLEL;

Prompt Index PSP_ATFPAYMENTS_TO_PROCESS_FK1;
CREATE INDEX PSP_ATFPAYMENTS_TO_PROCESS_FK1 ON PSP_ATFPAYMENTS_TO_PROCESS
(MONEY_MOVEMENT_TRANSACTION_FK, REALM_ID)
NOPARALLEL;

Prompt Index PSP_ATFPAYMENTS_TO_PROCESS_FK2;
CREATE INDEX PSP_ATFPAYMENTS_TO_PROCESS_FK2 ON PSP_ATFPAYMENTS_TO_PROCESS
(LAW_FK, REALM_ID)
NOPARALLEL;

Prompt Index PSP_ATFPAYMENTS_TO_PROCESS_FK3;
CREATE INDEX PSP_ATFPAYMENTS_TO_PROCESS_FK3 ON PSP_ATFPAYMENTS_TO_PROCESS
(COMPANY_FK, REALM_ID)
NOPARALLEL;

ALTER TABLE PSP_ATFPAYMENTS_TO_PROCESS
 ADD PRIMARY KEY
  (ATFPAYMENTS_TO_PROCESS_SEQ, REALM_ID)
  USING INDEX;

ALTER TABLE PSP_ATFPAYMENTS_TO_PROCESS
 ADD CONSTRAINT PSP_ATFPAYMENTS_TO_PROCESS_FK1 
  FOREIGN KEY (MONEY_MOVEMENT_TRANSACTION_FK, REALM_ID) 
  REFERENCES PSP_MONEY_MOVEMENT_TRANSACTION (MONEY_MOVEMENT_TRANSACTION_SEQ,REALM_ID);

ALTER TABLE PSP_ATFPAYMENTS_TO_PROCESS
 ADD CONSTRAINT PSP_ATFPAYMENTS_TO_PROCESS_FK2 
  FOREIGN KEY (LAW_FK, REALM_ID) 
  REFERENCES PSP_LAW (LAW_ID,REALM_ID);

ALTER TABLE PSP_ATFPAYMENTS_TO_PROCESS
 ADD CONSTRAINT PSP_ATFPAYMENTS_TO_PROCESS_FK3 
  FOREIGN KEY (COMPANY_FK, REALM_ID) 
  REFERENCES PSP_COMPANY (COMPANY_SEQ,REALM_ID);

PROMPT finishedDBUpgrade_002.000.013.006.sql