/
DECLARE
   sequence_exists PLS_INTEGER;
BEGIN
   SELECT COUNT(*) INTO sequence_exists
   FROM "USER_SEQUENCES"
   WHERE SEQUENCE_NAME = 'SEQ_EFTPS_FILE_SEQUENCE';
   IF sequence_exists = 0 THEN
    EXECUTE IMMEDIATE 'create sequence SEQ_EFTPS_FILE_SEQUENCE INCREMENT BY 1 MINVALUE 1 NOCYCLE';
   END IF;
end;
/